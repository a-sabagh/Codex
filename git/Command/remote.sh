#remote and add file to Git Hosting Services
git remote add <alias> <URL>
#remote remote 
git remote rm <alias>
#show fetch and push HTTPS link
git remote -v
#change remote URLs
git remote set-url <alias> <NEW_URL>
